# esutils

Go port of the npm package `esutils`, created by the uplift port toolchain.

| | |
|---|---|
| Original size | 409 LOC |
| Public API | `ast`, `code`, `keyword` |
| Needed by | doctrine, eslint, rules/func-name-matching.js, rules/utils/ast-utils.js |
| Module | `github.com/jclyons52/esutils-go` |

Workflow: transpile `original/` with `ts2go`, then hand-clean. Keep the parity test green.
